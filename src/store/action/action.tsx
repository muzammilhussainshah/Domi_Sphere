
import { Dispatch } from 'redux';

export const dummy = (inputValue: string, password: string) => {
    return async (dispatch: Dispatch) => {
        try {
        } catch (error: any) {
        }
    }
}
